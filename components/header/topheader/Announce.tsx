import styles from '../../../styles/components/announcement.module.scss';

const Announce = () => (
    <div className={styles.announceContainer}>
        <p>Grand opening, up to 15% off all items, Only 3 days left</p>
    </div>
);

export default Announce;
